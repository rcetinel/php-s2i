#!/bin/bash

mkdir ./gitrepo 
touch ./hellllllooo

echo "Before assembling"

cd ./gitrepo
git clone https://github.com/rcetinel/html 

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi
exit $rc
